{
  "name": "cockpit-adguard",
  "author": {
    "name": "Francesco Urso"
  },
  "keywords": [
    "cockpit",
    "adguard",
    "dns",
    "adblock",
    "dashboard"
  ],
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "prepare": "husky install",
    "branch-protection": "node -e \"if(process.env.GITHUB_REF?.includes('main')) { console.error('❌ Push bloccato su main'); process.exit(1); }\"",
    "build": "vite build",
    "build-cockpit": "node cockpit.config.js"
  },
  "devDependencies": {
    "fs-extra": "^11.3.1",
    "vite": "^5.0.0",
    "@commitlint/cli": "^19.8.1",
    "@commitlint/config-conventional": "^19.8.1",
    "husky": "^9.1.7",
    "lint-staged": "^16.1.6"
  },
  "dependencies": {
    "@commitlint/cli": "^19.8.1",
    "@commitlint/config-conventional": "^19.8.1",
    "husky": "^9.1.7",
    "lint-staged": "^16.1.6"
  },
  "lint-stages": {
    ".js": "eslint --fix"
  }
}
